- body_classes 'has-sticky-footer'
- content_for :base_content
  header
    = render 'navbar', class: 'is-success'
  main
    .container
      section.section
        == content_for?(:content) ? yield(:content) : yield
  = render 'footer'

== render 'layouts/base'
